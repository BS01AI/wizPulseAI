# Git 多项目管理指南

> WizPulseAI 多子项目 Git 监控完整方案

---

## 🎯 快速选择

| 需求 | 推荐脚本 | 特点 |
|------|---------|------|
| **日常检查** | `git-status-all.sh` | ⭐ 最常用，快速概览 |
| **查看改动** | `git-diff-all.sh` | 详细差异，支持多种模式 |
| **深入管理** | `git-interactive.sh` | 交互式，功能完整 |
| **视觉监控** | `git-dashboard.sh` | 图形化，适合演示 |

---

## 📊 方案对比

### 方案1: 快速概览 ⭐⭐⭐⭐⭐

```bash
./scripts/git-status-all.sh
```

**适用场景**：
- ✅ 每天开始工作前检查
- ✅ 提交代码前确认状态
- ✅ 快速了解哪个项目有改动

**输出信息**：
- 分支名称
- 清洁状态（✅ 干净 / ⚠️ 有变更）
- 未提交文件数
- 未推送/未拉取提交数
- 最近一次提交

**优点**：
- 最快速（1-2秒）
- 信息密度高
- 无需交互

**缺点**：
- 不显示具体改动内容

---

### 方案2: 详细差异 ⭐⭐⭐⭐

```bash
# 基础用法
./scripts/git-diff-all.sh

# 高级用法
./scripts/git-diff-all.sh --staged  # 已暂存
./scripts/git-diff-all.sh --all     # 所有改动
./scripts/git-diff-all.sh --stat    # 只看统计
```

**适用场景**：
- ✅ 提交前review代码
- ✅ 了解具体改了什么
- ✅ 对比不同项目的改动

**输出信息**：
- 每个文件的具体差异
- 增删行数统计
- 支持选择查看范围

**优点**：
- 信息最详细
- 支持多种模式
- 可以看到代码diff

**缺点**：
- 输出较长（如果改动多）
- 需要时间较长

---

### 方案3: 交互式管理 ⭐⭐⭐⭐

```bash
./scripts/git-interactive.sh
```

**适用场景**：
- ✅ 需要深入某个项目
- ✅ 执行多个git操作
- ✅ 探索性查看

**功能**：
- 菜单选择项目
- 执行常用git命令
  - git status
  - git diff / git diff --staged
  - git log
  - git branch
  - 自定义命令
- 查看所有项目概览

**优点**：
- 功能最全面
- 无需记命令
- 可执行自定义操作

**缺点**：
- 需要交互操作
- 不适合自动化

---

### 方案4: 图形化仪表盘 ⭐⭐⭐⭐⭐

```bash
# 单次查看
./scripts/git-dashboard.sh

# 实时监控
watch -n 5 ./scripts/git-dashboard.sh

# 彩色模式（推荐）
watch -n 5 -c ./scripts/git-dashboard.sh
```

**适用场景**：
- ✅ 演示项目状态
- ✅ 实时监控
- ✅ 视觉化展示

**输出特性**：
- 🎨 彩色表格
- 📊 进度条
- 📈 统计总览
- 🔄 实时刷新

**优点**：
- 最美观
- 信息一目了然
- 支持实时监控

**缺点**：
- 需要终端支持彩色
- watch模式占用终端

---

## 🎯 使用建议

### 日常开发工作流

```bash
# 1. 早上开始工作
./scripts/git-status-all.sh

# 2. 如果有改动，查看详情
./scripts/git-diff-all.sh --stat

# 3. 进入具体项目工作
cd auth-wizpulseai-com
# ... 开发工作 ...

# 4. 工作结束前检查
./scripts/git-dashboard.sh

# 5. 提交前review
./scripts/git-diff-all.sh
```

### 需要提交代码时

```bash
# 1. 检查所有项目状态
./scripts/git-status-all.sh

# 2. 查看具体改动
./scripts/git-diff-all.sh

# 3. 进入项目提交
cd auth-wizpulseai-com
git add .
git commit -m "feat: ..."
git push

# 4. 再次检查状态
cd ..
./scripts/git-status-all.sh
```

### 需要深入某个项目

```bash
# 使用交互式工具
./scripts/git-interactive.sh

# 选择项目 (1-3)
# 选择操作 (1-6)
# 查看结果
```

### 演示或监控

```bash
# 打开一个专门的终端窗口
watch -n 5 -c ./scripts/git-dashboard.sh

# 在其他窗口工作
# 仪表盘会自动刷新显示最新状态
```

---

## 💡 高级技巧

### 组合使用

```bash
# 快速检查 → 详细查看
./scripts/git-status-all.sh && ./scripts/git-diff-all.sh --stat

# 检查后自动打开交互工具（如果有改动）
./scripts/git-status-all.sh && ./scripts/git-interactive.sh
```

### 创建别名

在 `~/.bashrc` 或 `~/.zshrc` 中添加：

```bash
# WizPulseAI Git 别名
alias gs='cd ~/wizPulseAI && ./scripts/git-status-all.sh'
alias gd='cd ~/wizPulseAI && ./scripts/git-diff-all.sh'
alias gi='cd ~/wizPulseAI && ./scripts/git-interactive.sh'
alias gw='cd ~/wizPulseAI && watch -n 5 -c ./scripts/git-dashboard.sh'
```

然后就可以在任意目录执行：
```bash
gs  # 查看状态
gd  # 查看差异
gi  # 交互模式
gw  # 仪表盘
```

### 集成到 IDE

**VS Code Tasks** (`.vscode/tasks.json`):
```json
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Git: Check All Projects",
      "type": "shell",
      "command": "./scripts/git-status-all.sh",
      "problemMatcher": []
    }
  ]
}
```

---

## 🔧 自定义

### 修改项目列表

编辑脚本中的 `PROJECTS` 数组：

```bash
PROJECTS=(
  "auth-wizpulseai-com:Auth站点"
  "db-wizPulseAI-com:Dashboard站点"
  "wizPulseAI-com:Main站点"
  "new-project:新项目"  # 添加新项目
)
```

### 自定义输出格式

修改脚本中的显示函数，例如：

```bash
# 在 git-status-all.sh 中
# 修改颜色、图标、格式等
```

---

## 🆘 故障排查

### 问题1: 脚本无法执行

```bash
# 赋予执行权限
chmod +x scripts/git-*.sh
```

### 问题2: 颜色显示异常

```bash
# 使用 watch 的彩色模式
watch -n 5 -c ./scripts/git-dashboard.sh

# 或检查终端是否支持颜色
echo $TERM
```

### 问题3: 提示找不到 git

```bash
# 检查 git 是否安装
git --version

# 检查 PATH
which git
```

### 问题4: 子项目不是 Git 仓库

```bash
# 检查子项目目录是否有 .git 文件夹
ls -la auth-wizpulseai-com/.git

# 如果没有，初始化 Git
cd auth-wizpulseai-com
git init
```

---

## 📚 相关文档

- [scripts/README.md](../../scripts/README.md) - 完整脚本文档
- [../README.md](../../z-系统配置/README.md) - 项目总览
- [LOCAL_TEST_GUIDE.md](LOCAL_TEST_GUIDE.md) - 本地测试指南

---

## 🎉 总结

**推荐使用**：

1. **每天必用**: `git-status-all.sh` - 快速检查
2. **提交前用**: `git-diff-all.sh` - 代码review
3. **深度管理**: `git-interactive.sh` - 完整功能
4. **演示监控**: `git-dashboard.sh` - 视觉化

选择适合你当前需求的工具，提高多项目管理效率！

---

最后更新: 2025-10-30
