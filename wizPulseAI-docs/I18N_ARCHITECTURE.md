# WizPulseAI 多语言系统架构文档

## 📋 目录

1. [系统概览](#系统概览)
2. [技术架构](#技术架构)
3. [实施方案](#实施方案)
4. [跨站点同步](#跨站点同步)
5. [文件结构](#文件结构)
6. [最佳实践](#最佳实践)

---

## 系统概览

### 支持的语言

WizPulseAI平台统一支持4种语言：

| 语言代码 | 语言名称 | 国旗 | RTL支持 |
|---------|---------|------|---------|
| `ja` | 日本語 | 🇯🇵 | ❌ |
| `en` | English | 🇺🇸 | ❌ |
| `ar` | العربية | 🇸🇦 | ✅ |
| `zh-TW` | 繁體中文 | 🇹🇼 | ❌ |

**默认语言**：日语 (ja)

---

## 技术架构

### 混合策略架构

WizPulseAI采用**差异化多语言方案**，根据各站点特性选择最优技术：

```
┌─────────────────────────────────────────────────────────┐
│                  WizPulseAI 多语言系统                    │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  Main站点               Auth站点            Dashboard站点 │
│  ┌──────────┐         ┌──────────┐        ┌──────────┐  │
│  │next-intl │         │SimpleI18n│        │SimpleI18n│  │
│  │Framework │         │Provider  │        │Provider  │  │
│  ├──────────┤         ├──────────┤        ├──────────┤  │
│  │URL路由   │         │Cookie驱动│        │Cookie驱动│  │
│  │/ja/xxx   │         │动态切换  │        │动态切换  │  │
│  │SSR优化   │         │Client刷新│        │Client刷新│  │
│  │SEO完整   │         │轻量快速  │        │轻量快速  │  │
│  └──────────┘         └──────────┘        └──────────┘  │
│       │                     │                   │         │
│       └──────────┬──────────┴───────────────────┘         │
│                  │                                         │
│        Cookie同步（.wizpulseai.com）                      │
│              NEXT_LOCALE=ja                               │
└─────────────────────────────────────────────────────────┘
```

### 架构决策

| 站点 | 技术方案 | 理由 |
|------|---------|------|
| **Main** | next-intl + URL路由 | • SEO需求（/ja/products可被搜索引擎索引）<br>• 内容丰富（978行/语言）<br>• SSR性能优化 |
| **Auth** | SimpleI18nProvider | • 无SEO需求（登录页不索引）<br>• UI简单（75条翻译）<br>• 快速轻量 |
| **Dashboard** | SimpleI18nProvider | • 无SEO需求（管理后台）<br>• UI简单（100条翻译）<br>• 快速轻量 |

**关键原则**：不为了统一而统一，根据实际需求选择最优方案。

---

## 实施方案

### 1. 共享基础设施 (`/shared/i18n/`)

所有站点共享的核心模块：

```typescript
// shared/i18n/config.ts
export const LOCALES = ['ja', 'en', 'ar', 'zh-TW'] as const;
export const DEFAULT_LOCALE = 'ja';
export const COOKIE_NAME = 'NEXT_LOCALE';
export const COOKIE_DOMAIN =
  process.env.NODE_ENV === 'production'
    ? '.wizpulseai.com'
    : '.localhost';
```

**文件清单**：
- `config.ts` - 统一配置（语言列表、Cookie常量）
- `cookie-utils.ts` - Cookie读写工具
- `types.ts` - TypeScript类型定义
- `SimpleI18nProvider.tsx` - 轻量级i18n Provider
- `index.ts` - 统一导出

### 2. Auth站点实施

**翻译文件结构**：
```
auth-wizpulseai-com/src/messages/
├── ja.json    (120行)
├── en.json    (120行)
├── ar.json    (120行)
├── zh-TW.json (120行)
└── index.ts
```

**集成示例**：
```typescript
// app/layout.tsx
import { SimpleI18nProvider } from '@/shared/i18n';
import messages from '@/messages';

export default function RootLayout({ children }) {
  return (
    <html lang="ja">
      <body>
        <SimpleI18nProvider messages={messages}>
          {children}
        </SimpleI18nProvider>
      </body>
    </html>
  );
}
```

**使用示例**：
```typescript
// 任何组件
import { useTranslation } from '@/shared/i18n';

function LoginForm() {
  const { t } = useTranslation();
  return <h1>{t.auth.signIn.title}</h1>; // → "ログイン"
}
```

### 3. Dashboard站点实施

**翻译文件结构**（与Auth相同）：
```
db-wizPulseAI-com/src/messages/
├── ja.json    (72行)
├── en.json    (72行) ⭐⭐⭐⭐⭐ AI翻译
├── ar.json    (72行) ⭐⭐⭐⭐⭐ AI翻译
├── zh-TW.json (72行) ⭐⭐⭐⭐⭐ AI翻译
└── index.ts
```

**AI翻译流程**：
- Layer 1: 初译（日语 → 目标语言）
- Layer 2: 校对（8-11处改进）
- Layer 3: 润色（最终打磨）

### 4. Main站点配置

**保持next-intl架构**（不修改）：
```
wizPulseAI-com/src/messages/
├── ja.json    (978行)
├── en.json    (978行)
├── ar.json    (978行)
└── zh-TW.json (978行)
```

**特点**：
- URL路由：`/ja/products`, `/en/products`
- SSR优化
- SEO友好
- 完整翻译覆盖

---

## 跨站点同步

### Cookie机制

**Cookie配置**：
```typescript
name: 'NEXT_LOCALE'
domain: '.wizpulseai.com'  // 生产环境
domain: '.localhost'        // 本地开发
expires: 365天
sameSite: 'lax'
secure: true (生产)
```

**同步流程**：
```
用户访问 Main站点 → 选择日语 → 写Cookie
              (.wizpulseai.com, NEXT_LOCALE=ja)
                         ↓
用户点击登录 → 跳转Auth站点 → 读Cookie → 显示日语界面
                         ↓
登录成功 → 跳转Dashboard → 读Cookie → 显示日语界面
```

**关键特性**：
- ✅ 纯前端同步（无需API调用）
- ✅ 跨子域名共享（3个站点互通）
- ✅ 刷新页面保持
- ✅ 环境自动切换

---

## 文件结构

### 项目完整结构

```
wizPulseAI/
├── shared/                          # 共享模块
│   ├── i18n/                       # i18n核心
│   │   ├── config.ts               # 统一配置 ✅
│   │   ├── cookie-utils.ts         # Cookie工具 ✅
│   │   ├── SimpleI18nProvider.tsx  # Provider ✅
│   │   ├── types.ts                # 类型定义 ✅
│   │   └── index.ts                # 统一导出 ✅
│   ├── components/                 # 共享组件
│   │   └── LanguageSwitcher.tsx    # 语言切换器 ✅
│   └── README.md                   # 使用文档 ✅
│
├── auth-wizpulseai-com/            # Auth站点
│   ├── src/
│   │   ├── messages/               # 翻译文件 ✅
│   │   │   ├── ja.json   (120行)
│   │   │   ├── en.json   (120行)
│   │   │   ├── ar.json   (120行)
│   │   │   ├── zh-TW.json (120行)
│   │   │   └── index.ts
│   │   └── app/layout.tsx          # SimpleI18nProvider集成 ✅
│   └── tsconfig.json               # @/shared/*路径别名 ✅
│
├── db-wizPulseAI-com/              # Dashboard站点
│   ├── src/
│   │   ├── messages/               # 翻译文件 ✅
│   │   │   ├── ja.json   (72行)
│   │   │   ├── en.json   (72行) ⭐AI翻译
│   │   │   ├── ar.json   (72行) ⭐AI翻译
│   │   │   ├── zh-TW.json (72行) ⭐AI翻译
│   │   │   └── index.ts
│   │   └── app/layout.tsx          # SimpleI18nProvider集成 ✅
│   └── tsconfig.json               # @/shared/*路径别名 ✅
│
├── wizPulseAI-com/                 # Main站点
│   ├── src/
│   │   ├── messages/               # 翻译文件 ✅
│   │   │   ├── ja.json   (978行)
│   │   │   ├── en.json   (978行)
│   │   │   ├── ar.json   (978行)
│   │   │   └── zh-TW.json (978行)
│   │   └── shared/config/locales.ts # 配置文件 ✅
│   └── .env.local                  # Cookie域名配置 ✅
│
└── wizPulseAI-docs/                # 技术文档
    ├── I18N_ARCHITECTURE.md        # 本文档 ✅
    ├── TRANSLATION_GLOSSARY.md     # 术语统一表 ✅
    └── TRANSLATION_GUIDE.md        # 添加新语言指南 ✅
```

---

## 最佳实践

### 1. 翻译文件管理

**JSON结构规范**：
```json
{
  "nav": {
    "dashboard": "ダッシュボード",
    "settings": "設定"
  },
  "auth": {
    "signIn": {
      "title": "ログイン",
      "emailLabel": "メールアドレス"
    }
  }
}
```

**命名规则**：
- 使用嵌套结构（最多3层）
- key用camelCase
- 保持4个语言文件结构一致
- 占位符格式：`{{variableName}}`

### 2. 组件使用规范

**推荐做法** ✅：
```typescript
const { t } = useTranslation();
<button>{t.actions.save}</button>
```

**避免做法** ❌：
```typescript
<button>保存</button>  // 硬编码文本
```

### 3. 新增翻译流程

1. 在`ja.json`中添加日语翻译（基准）
2. 使用`translation-manager` agent批量翻译
3. 人工审核关键术语
4. 4个语言文件保持结构一致

### 4. Cookie最佳实践

**开发环境**：
```env
NEXT_PUBLIC_COOKIE_DOMAIN=.localhost
```

**生产环境**：
```env
NEXT_PUBLIC_COOKIE_DOMAIN=.wizpulseai.com
```

**验证方法**：
```javascript
// 浏览器控制台
document.cookie.split(';').find(c => c.includes('NEXT_LOCALE'))
// → " NEXT_LOCALE=ja"
```

### 5. RTL布局处理

阿拉伯语自动启用RTL布局：

```typescript
// shared/i18n/config.ts
export const RTL_LOCALES: Locale[] = ['ar'];

// 使用
import { isRTL } from '@/shared/i18n';
<div dir={isRTL(locale) ? 'rtl' : 'ltr'}>
```

---

## 性能优化

### 1. Main站点（next-intl）

- ✅ SSR渲染（首屏快速）
- ✅ 翻译文件懒加载
- ✅ URL路由缓存

### 2. Auth/Dashboard（SimpleI18nProvider）

- ✅ 翻译文件打包时已包含（零加载时间）
- ✅ Client端简单快速
- ✅ 刷新页面才生效（无需复杂状态管理）

---

## 维护指南

### 添加新语言

详见 [TRANSLATION_GUIDE.md](./TRANSLATION_GUIDE.md)

### 术语统一

详见 [TRANSLATION_GLOSSARY.md](./TRANSLATION_GLOSSARY.md)

### 调试技巧

1. **检查Cookie**：
```bash
# Chrome DevTools → Application → Cookies
# 查看 Domain 列应该是 .wizpulseai.com 或 .localhost
```

2. **验证翻译加载**：
```javascript
// 组件中
const { t, locale } = useTranslation();
console.log('当前语言:', locale);
console.log('翻译对象:', t);
```

3. **测试跨站点同步**：
- Main切换语言 → 检查Cookie写入
- 跳转Auth → 检查Cookie读取
- 验证语言一致

---

## 常见问题

### Q: 为什么不统一使用next-intl？

**A**: 差异化需求：
- Main需要SEO → next-intl（URL路由）
- Auth/Dashboard不需要SEO → SimpleI18nProvider（更轻量）

### Q: Cookie跨域不生效怎么办？

**A**: 检查：
1. 域名配置是否正确（`.localhost` vs `.wizpulseai.com`）
2. sameSite设置是否为`lax`
3. 生产环境是否启用`secure`

### Q: 如何添加新语言？

**A**: 参考 [TRANSLATION_GUIDE.md](./TRANSLATION_GUIDE.md)

### Q: 翻译质量如何保证？

**A**: 使用`translation-manager` agent的3层翻译流程：
- Layer 1: 初译
- Layer 2: 校对（8-11处改进）
- Layer 3: 润色

---

## 项目完成度

| Phase | 站点 | 完成度 | 说明 |
|-------|------|--------|------|
| Phase 1 | Shared | 100% | 共享基础设施完成 ✅ |
| Phase 2 | Auth | 75% | 核心完成，UI优化待改进 ⚠️ |
| Phase 3 | Dashboard | 85% | 核心完成，组件集成待优化 ⚠️ |
| Phase 4 | Main | 100% | 清理完成，配置完美 ✅ |
| Phase 5 | 文档 | 100% | 技术文档完整 ✅ |

**总体完成度**：90%

**后续优化**：
- Auth page.tsx完全重构（从975行拆分）
- Dashboard组件翻译集成
- 删除旧的LanguageProvider

---

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0 | 2025-11-14 | 初版发布 |

---

**文档维护**：WizPulseAI开发团队
**最后更新**：2025-11-14
