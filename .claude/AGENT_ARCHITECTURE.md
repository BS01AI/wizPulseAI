# WizPulseAI Sub-agent 组织架构图

## 📊 整体架构（8个Sub-agent）

```
                    ┌──────────────────────────────┐
                    │   主 AI (Main Agent)         │
                    │   Claude Sonnet 4.5          │
                    │                              │
                    │  • 任务分析和规划            │
                    │  • 代码编写和修改            │
                    │  • Sub-agent 智能调度        │
                    │  • 结果整合和汇报            │
                    └──────────────┬───────────────┘
                                   │
                                   │ 自动调度/手动指定
                                   │
        ┌──────────────────────────┼──────────────────────────┐
        │                          │                          │
        │    Layer 1: 测试层       │   Layer 2: 开发层       │   Layer 3: 审查优化层
        │                          │                          │
    ┌───▼──────┐  ┌───────────┐  ┌▼─────────────┐  ┌─────────▼────┐  ┌────────────────┐
    │ sso-     │  │ stripe-   │  │ cross-site-  │  │ multi-site-  │  │ security-      │
    │ tester   │  │ tester    │  │ validator    │  │ coder        │  │ auditor        │
    │ Sonnet   │  │ Sonnet    │  │ Sonnet       │  │ Sonnet       │  │ Opus ⭐        │
    └──────────┘  └───────────┘  └──────────────┘  └──────────────┘  └────────────────┘
         │             │               │                  │                   │
         │             │               │                  │                   │
    Playwright    Stripe MCP      Grep/Read         Read/Write/Edit      Read/Grep
    Chrome Dev                                                           Supabase MCP

    ┌────────────┐  ┌────────────────┐  ┌───────────────┐
    │ supabase-  │  │ performance-   │  │ git-manager   │
    │ manager    │  │ analyzer       │  │ Haiku         │
    │ Sonnet     │  │ Sonnet         │  └───────────────┘
    └────────────┘  └────────────────┘         │
         │                  │                   │
    Supabase MCP    Chrome DevTools       Git 命令
                    Playwright            4个仓库管理
```

---

## 🎯 核心角色（8个Sub-agent + 主AI）

### 主 AI (Main Agent)
- **模型**: Claude Sonnet 4.5
- **职责**: 任务分析、代码编写、Sub-agent调度、结果整合
- **决策权**: 判断何时调用哪个 Sub-agent

---

### Layer 1: 测试层 (3个)

#### 1️⃣ sso-tester (SSO 测试专家)
- **模型**: Sonnet
- **职责**: 自动化测试SSO登录流程、验证Cookie域、检查认证状态
- **工具**: Playwright、Chrome DevTools
- **触发**: 修改认证相关代码后

#### 2️⃣ stripe-tester (支付测试专家)
- **模型**: Sonnet
- **职责**: 测试Stripe支付流程、订阅管理、Webhook验证
- **工具**: Stripe MCP、Playwright
- **触发**: 修改支付或订阅相关代码后

#### 3️⃣ cross-site-validator (跨站点验证专家)
- **模型**: Sonnet
- **职责**: 验证三站点配置一致性、环境变量、依赖版本
- **工具**: Read、Grep、Glob、Bash
- **触发**: 修改共享配置或环境变量后

---

### Layer 2: 开发层 (2个)

#### 4️⃣ multi-site-coder (多站点代码编写专家)
- **模型**: Sonnet
- **职责**: 编写符合三站点架构的代码、深入理解SSO机制
- **工具**: Read、Write、Edit、Glob、Grep、Bash
- **触发**: 需要跨站点功能或修改共享组件时

#### 5️⃣ supabase-manager (数据库管理专家)
- **模型**: Sonnet
- **职责**: 管理Supabase数据库、查询日志、执行SQL、生成类型
- **工具**: Supabase MCP、Read、Bash
- **触发**: 需要查询数据库或检查日志时

---

### Layer 3: 审查优化层 (2个)

#### 6️⃣ security-auditor (安全审计专家) ⭐ 使用Opus
- **模型**: Opus（最强推理能力）
- **职责**: 深度安全审计、战略风险评估、架构安全分析
- **工具**: Read、Grep、Glob、Supabase MCP、Bash
- **触发**: 修改认证、支付、Cookie或准备部署前
- **特点**: 提供架构级别的安全建议和防御策略

#### 7️⃣ performance-analyzer (性能分析专家)
- **模型**: Sonnet
- **职责**: 分析页面性能、Bundle大小、Core Web Vitals
- **工具**: Chrome DevTools、Playwright、Read、Grep、Bash
- **触发**: 优化性能、减小体积、提升用户体验时

---

### 特殊角色

#### 8️⃣ git-manager (Git 管理专家)
- **模型**: Haiku（简单任务用经济模型）
- **职责**: 管理4个Git仓库、智能判断提交时机、理解业务阶段
- **工具**: Bash、Git命令
- **触发**: 功能完成 + 测试通过后

---

## 🔄 工作流程

### 典型场景：修改 Auth 站点登录逻辑

```
┌──────────────┐
│  用户需求    │  "修改登录表单，添加邮箱验证"
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────┐
│  主 AI                           │
│  1. 分析需求                     │
│  2. 修改代码                     │
│     - src/components/Login.tsx  │
│     - 添加邮箱验证逻辑           │
│  3. 判断需要测试                 │
└──────┬───────────────────────────┘
       │
       │ 调用
       ▼
┌──────────────────────────────────┐
│  sso-tester (Sub-agent)          │
│  1. 运行自动化测试               │
│  2. 验证登录流程                 │
│  3. 检查 Cookie 和 Session       │
│  4. 生成测试报告                 │
└──────┬───────────────────────────┘
       │
       │ 返回结果
       ▼
┌──────────────────────────────────┐
│  主 AI                           │
│  1. 收到测试报告: ✅ 所有测试通过 │
│  2. 询问用户: "是否提交？"       │
└──────┬───────────────────────────┘
       │
       │ 用户确认
       ▼
┌──────────────────────────────────┐
│  git-manager (Sub-agent)         │
│  1. 检测 auth-wizpulseai-com 改动│
│  2. 提交: "feat: 添加邮箱验证"    │
│  3. 推送到 GitHub                │
└──────┬───────────────────────────┘
       │
       │ 返回结果
       ▼
┌──────────────────────────────────┐
│  主 AI                           │
│  汇报: "✅ 所有改动已提交并推送"  │
└──────────────────────────────────┘
       │
       ▼
┌──────────────┐
│  用户        │  任务完成！
└──────────────┘
```

---

## 🧠 调度机制

### 自动调度规则

```
用户操作                       主 AI 判断                  调用 Sub-agent
──────────────────────────────────────────────────────────────────
修改 Auth 登录逻辑        →   需要验证 SSO 功能      →   sso-tester
修改 Dashboard 认证       →   需要验证 SSO 功能      →   sso-tester
修改 Cookie 配置          →   需要验证 SSO 功能      →   sso-tester
功能开发完成              →   测试通过 + 用户确认    →   git-manager
Bug 修复完成              →   测试通过 + 用户确认    →   git-manager
添加新产品页面            →   无需测试，直接提交     →   git-manager
```

### 手动调用

```bash
# 用户可以直接指定 Sub-agent
"用 sso-tester 测试一下登录"     → 强制调用 sso-tester
"用 git-manager 提交所有改动"    → 强制调用 git-manager
"检查 Git 状态"                  → 调用 git-manager (status)
```

---

## 📦 仓库管理架构

### 当前架构 (Multi-repo)

```
┌─────────────────────────────────────────┐
│  主仓库: wizPulseAI                     │
│  git@github.com:BS01AI/wizPulseAI.git   │
│                                         │
│  管理内容:                              │
│  • 脚本 (start-all.sh, stop-all.sh)    │
│  • 文档 (CLAUDE.md, README.md)         │
│  • Sub-agent 配置 (.claude/agents/)    │
│  • 工具脚本 (git-push-all.sh)          │
└─────────────────────────────────────────┘
           │
           │ 包含但不追踪（.gitignore）
           │
    ┌──────┴──────┬──────────────┬──────────────┐
    │             │              │              │
    ▼             ▼              ▼              ▼
┌────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│ Auth   │  │Dashboard │  │  Main    │  │  未来... │
│ 站点   │  │  站点    │  │  站点    │  │  站点    │
└────────┘  └──────────┘  └──────────┘  └──────────┘
    │             │              │              │
    ▼             ▼              ▼              ▼
独立仓库1     独立仓库2      独立仓库3      独立仓库N
```

### git-manager 管理的仓库

| 仓库名称 | Git URL | 提交频率 (当前阶段) |
|---------|---------|-------------------|
| auth-wizpulseai-com | BS01AI/auth-wizpulseai-com.git | 低频 (bug修复) |
| db-wizPulseAI-com | BS01AI/db-wizPluseAI-com.git | 低频 (bug修复) |
| wizPulseAI-com | BS01AI/wizPulseAI-com.git | 🔥 高频 (内容扩展) |
| 主仓库 | BS01AI/wizPulseAI.git | 中频 (文档/脚本) |

---

## 🎯 业务阶段理解

### 阶段1: 基础架构期 (已完成)
```
特点: Auth/Dashboard/Main 三站点频繁修改
Git行为: 三个子站点都经常提交
```

### 阶段2: 内容扩展期 (当前) ⭐
```
特点: 主要在 Main 站点追加内容
Git行为:
  • wizPulseAI-com (Main)     → 高频提交 🔥
  • 主仓库 (文档/脚本)         → 中频提交
  • auth/dashboard            → 低频提交
```

### 阶段3: 新服务扩展期 (未来)
```
特点: 添加新的独立站点
Git行为: 可扩展到 5、6、7 个仓库
```

---

## 🔧 技术栈

### 主 AI
- **Claude Sonnet 4.5** - 强大的代码生成和推理能力
- **Tools**: Read, Write, Edit, Bash, Grep, Glob, MCP Tools

### sso-tester
- **Claude Haiku** - 快速执行测试任务
- **Tools**:
  - `mcp__playwright` - 浏览器自动化
  - `mcp__chrome-devtools` - Chrome 调试
  - `Read`, `Bash` - 文件读取和命令执行

### git-manager
- **Claude Haiku** - 快速执行Git操作
- **Tools**:
  - `Bash` - Git 命令执行
  - `Read` - 读取配置和状态
  - `Grep` - 搜索和分析

---

## 🤖 模型策略

### Opus（最强思维）- 1个
- **security-auditor**: 深度安全审计需要最强推理能力

### Sonnet（主力）- 6个
- **sso-tester**: SSO测试需要可靠判断
- **stripe-tester**: 支付测试需要准确性
- **cross-site-validator**: 配置验证需要理解能力
- **multi-site-coder**: 代码编写需要质量保证
- **supabase-manager**: 数据库操作需要准确性
- **performance-analyzer**: 性能分析需要专业性

### Haiku（经济型）- 1个
- **git-manager**: Git操作相对简单，使用经济模型

**成本优化原则**：
- 测试和开发用Sonnet（质量优先）
- 战略决策用Opus（深度思考）
- 简单重复任务用Haiku（成本优化）

---

## 📈 完成状态

### ✅ 已完成 (8个)
```
Layer 1: 测试层
├── sso-tester           ✅ Sonnet
├── stripe-tester        ✅ Sonnet
└── cross-site-validator ✅ Sonnet

Layer 2: 开发层
├── multi-site-coder     ✅ Sonnet
└── supabase-manager     ✅ Sonnet

Layer 3: 审查优化层
├── security-auditor     ✅ Opus ⭐
└── performance-analyzer ✅ Sonnet

特殊角色
└── git-manager          ✅ Haiku
```

### 📝 未来可能扩展
```
站点管理
├── deployment-manager   📝 部署管理（Vercel/CI/CD）
└── env-manager          📝 环境变量管理

用户管理
├── user-analytics       📝 用户行为分析
└── email-template-mgr   📝 邮件模板管理

内容管理
├── content-optimizer    📝 SEO优化
└── i18n-manager         📝 多语言管理
```

---

## 💡 设计原则

### 1. 单一职责
每个 Sub-agent 只负责一件事，做到极致。

### 2. 智能调度
主 AI 根据上下文自动判断，用户无需手动指定。

### 3. 可扩展性
架构支持未来添加更多 Sub-agent 和仓库。

### 4. 成本优化
测试和Git操作使用 Haiku（便宜），主逻辑使用 Sonnet（强大）。

### 5. 业务理解
Sub-agent 理解项目的业务发展阶段，智能调整行为。

---

## 📝 文件位置

```
.claude/
├── agents/
│   ├── README.md                  # Sub-agent 使用指南
│   ├── sso-tester.md             # 1. SSO 测试专家
│   ├── stripe-tester.md          # 2. 支付测试专家
│   ├── cross-site-validator.md   # 3. 跨站点验证专家
│   ├── multi-site-coder.md       # 4. 多站点代码编写专家
│   ├── supabase-manager.md       # 5. 数据库管理专家
│   ├── security-auditor.md       # 6. 安全审计专家 (Opus)
│   ├── performance-analyzer.md   # 7. 性能分析专家
│   └── git-manager.md            # 8. Git 管理专家
└── AGENT_ARCHITECTURE.md         # 本文档 (组织架构图)

scripts/
└── git-push-all.sh               # Git 管理工具脚本

CLAUDE.md                         # 项目记忆主文件
```

---

## 🚀 快速开始

### 1. 查看 Sub-agent 状态
```bash
# Claude Code 会自动识别 .claude/agents/ 目录中的配置
ls -la .claude/agents/
```

### 2. 让主 AI 自动调度 (推荐)
```bash
"修改 Auth 站点的登录逻辑"
# 主 AI 会自动调用 sso-tester 测试，然后调用 git-manager 提交
```

### 3. 手动调用 Sub-agent
```bash
"用 sso-tester 测试一下登录"
"用 git-manager 检查所有仓库状态"
```

### 4. 使用工具脚本
```bash
./git-push-all.sh status   # 手动检查 Git 状态
```

---

**创建时间**: 2025-11-10
**维护者**: Claude AI
**项目**: WizPulseAI SSO System
**版本**: v1.0
